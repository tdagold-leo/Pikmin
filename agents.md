# 專案導航與 Agent 作業規範 (Project Navigation & Agent Rules)

這份文件是本專案的基礎架構地圖與行為準則。身為 AI Agent，在執行任何讀取、寫入或重構任務前，請務必優先理解並嚴格遵守以下規範。請採用漸進式探索（Progressive Disclosure），先閱讀相關文件再進行修改。

## 1. 核心開發原則 (Core Principles)
在生成或修改任何程式碼時，必須遵守本專案的底線：

* **最小更動原則 (Minimal Impact)**：只修改完成任務所必需的程式碼。禁止擅自重構與當前任務無關的模組或函數。
* **維持既有風格 (Style Consistency)**：模仿並延續專案中現有的命名慣例、縮排與註解風格。不要引入你偏好的全新設計模式，除非任務明確要求。
* **依賴管理 (Dependency Control)**：禁止擅自引入新的第三方函式庫或套件。如果任務絕對需要新套件，請先在規劃階段提出說明。
* **禁止自動部署 (No Auto-Deploy)**：在完成任何程式碼修改後，必須先讓使用者在本機端確認功能正常，獲得明確許可後，才能將程式碼上傳或推送到 GitHub。

## 2. 標準工作流程 (Standard Workflow)
請按照以下步驟執行你的任務：

1. **探索與規劃 (Explore & Plan)**：先讀取相關的檔案與文件，理解現有邏輯。提出修改計畫。
2. **精準執行 (Execute)**：根據計畫修改程式碼。確保邏輯完整且括號/標籤閉合。
3. **自我驗證 (Validate)**：執行專案現有的測試指令（如 `npm test`, `pytest`, cargo test 等）。
4. **清理與回報 (Report)**：確保沒有殘留的除錯訊息（如 console.log 或 `print`）。條列出你實際修改的檔案與影響範圍。

## 3. 地標與座標搜尋規範 (Landmark & Coordinate Search Rules)
當使用者要求尋找特定地標或明信片的座標時，AI Agent 必須遵循以下搜尋順序：
1. **優先搜尋皮克敏社團資料**：搜尋社群回報的純點地圖、玩家整理的 Excel 表單、HackMD 筆記、FB 社團、巴哈姆特討論區等。
2. **再搜尋其他網路來源**：若社群無相關資料，再透過 Google 地圖、地方部落格、新聞、政府門牌整合檢索系統等管道尋找精確經緯度。